pila = [] #Aqui se declara la lista, que se trabaja con el nombre de "pila", que se trabajara como una pila.
item = 0 #Aqui se declara una variable que tomara el valor que el usuario le indique, para insertar en la pila.
i = 0 #Esta es la variable que se utilizara como indice, y la declararemos como global para poder utilizarla en el metodo push sin problemas.
limite = 5 #Esta variable la utilizaremos como el limite de nuestra pila
indice = 0 #Esta variable la utilizaremos como indice en la sentencia if, tomando el valor del indice dentro del metodo.
    
def push(i,item,limite):
	if i < limite:
		pila.append(item) 
    	i=i+1 
    	return i 

while i < limite:
	print("Ingresa tu numero")
	item = int(input())

	i = push(i,item,limite)


print(pila) #Aqui imprimimos la pila para ver como queda, que, en este caso queda llena.


								







     


    






    
   
